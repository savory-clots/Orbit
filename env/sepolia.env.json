{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Network and explorer configuration for Orbit on Base Sepolia.",
  "environment": {
    "name": "base-sepolia",
    "chainId": 84532,
    "rpc": {
      "default": "https://sepolia.base.org",
      "fallbacks": [
        "https://base-sepolia-rpc.publicnode.com"
      ]
    },
    "explorer": {
      "name": "BaseScan Sepolia",
      "url": "https://sepolia.basescan.org"
    }
  },
  "defaults": {
    "rpcTimeoutMs": 10000,
    "retries": 2,
    "readOnly": true
  },
  "notes": {
    "usage": "Loaded by local scripts and tooling to target Base Sepolia.",
    "warning": "No private keys or secrets should be stored in this file."
  }
}
